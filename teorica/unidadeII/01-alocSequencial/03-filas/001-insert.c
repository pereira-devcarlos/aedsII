#include <stdio.h>
#include <stdlib.h>
#define M 5 // Tamanho máximo da fila

typedef struct {
    int chave;
    int valor;
} No;


int main(){
    No F[M]; // Nó da fila
    int f = -1; // Inicio da fila
    int r = -1; // Retaguarda
    
    return 0;
}
